<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-6 col-md-offset-3">

      <h2 class="signup-form-title">Sign up</h2>

      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>


          <div class="form-names">
            <div class="first-name">
              <%= f.input :first_name, required: true, placeholder: "First Name", label: false, input_html: { class: 'rounded' } %>
            </div>
            <div class="last-name">
              <%= f.input :last_name, required: true, placeholder: "Last Name", label: false, input_html: { class: 'rounded' }  %>
            </div>
          </div>

          <%= f.input :username, required: true, placeholder: "Username", label: false, input_html: { class: 'rounded' } %>
          <%= f.input :address, required: true, placeholder: "Address", label: false, input_html: { class: 'rounded' }  %>
          <%= f.input :email, required: true, autofocus: true, placeholder: "Email", label: false, input_html: { class: 'rounded' }  %>
          <%= f.input :password, required: true, placeholder: "Password", hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length), label: false, input_html: { class: 'rounded' }  %>
          <%= f.input :password_confirmation, required: true, placeholder: "Confirm Password", label: false, input_html: { class: 'rounded' }  %>
          <%= f.input :photo %>
          <%= f.input :photo_cache, as: :hidden %>

        <div class="form-actions">
          <%= f.button :submit, "Sign up", class: "btn btn-danger", input_html: { class: 'rounded' }  %>


      <% end %>

      <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>

